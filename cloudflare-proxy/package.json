{
  "name": "eggo-fund-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "kv:create": "wrangler kv:namespace create CACHE",
    "kv:list": "wrangler kv:key list --binding CACHE"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
