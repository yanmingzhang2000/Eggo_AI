name = "eggo-fund-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

# KV 命名空间（部署后填入 ID）
# wrangler kv:namespace create CACHE
# [kv_namespaces]
# binding = "CACHE"
# id = "your-kv-namespace-id"

# 环境变量
[vars]
API_KEY = "eggo-proxy-2026"   # 改成你自己的密钥
ALLOWED_ORIGINS = "https://eggo-ai.onrender.com,http://localhost:5173"
